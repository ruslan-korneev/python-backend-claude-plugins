{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read file_path; if echo \"$file_path\" | grep -q '\\.py$'; then mypy \"$file_path\" --no-error-summary 2>/dev/null | head -20; fi; } || true"
          }
        ]
      }
    ]
  }
}
